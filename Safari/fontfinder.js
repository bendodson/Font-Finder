
/*

function handleMessage(msgEvent) {
   	var messageName = msgEvent.name;
   	var messageData = msgEvent.message;
   	if (messageName === "fontfinder") {
		fontFinder();
	}
}

safari.self.addEventListener("message", handleMessage, false);
*/